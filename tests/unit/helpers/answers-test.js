import { answers } from 'q-and-a/helpers/answers';
import { module, test } from 'qunit';

module('Unit | Helper | answers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answers([42]);
  assert.ok(result);
});
