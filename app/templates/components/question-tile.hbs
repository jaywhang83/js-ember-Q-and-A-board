
<div class="row">
  <div class="col s6">
    <div class="card-panel">
      {{#link-to 'question' question.id}}
      <h5 class="card-title">{{question.title}}</h5>
      <hr>
      {{answers question}}
      {{/link-to}}
      <br><br>
      {{question.author}}
      <br>
      {{question.date}}
    </div>
  </div>
</div>
